import React from "react";

export default function WorkTogether() {
  return (
    <footer className="flex flex-col items-center px-6 py-10 bg-gray-900 text-white">
      <h2 className="text-2xl font-semibold mb-4">Let's Work Together</h2>
      <p className="text-lg text-center max-w-2xl mb-6">
        To serve the community, the community itself should work together. Only the community can save its culture and heritage.
      </p>
    </footer>
  );
}
